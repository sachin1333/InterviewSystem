# Production uses one OpenAI model for every LLM task.
# Runtime values are loaded from .env:
#   OPENAI_MODEL=gpt-5.5
#   OPENAI_REASONING_EFFORT=low
model:
  provider: "openai"
  default_model: "gpt-5.5"
  reasoning_effort: "low"
