# RCA Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the five RCA-confirmed failures in the interview web flow.

**Architecture:** Problem-bank sessions must be governed by problem-boundary events, not the legacy global target-answer FSM. UI and voice fixes are additive and preserve the event model. Latency improvements prioritize removing unintended scorer calls from the active interview request path.

**Tech Stack:** FastAPI, Jinja templates, vanilla JS, SQLite event log, pytest, mypy, ruff.

---

## Tasks
1. Add regression tests before production changes.
2. Fix Phase-2 problem progression and production examiner wiring.
3. Fix duplicate opener and input clearing UX.
4. Fix voice protocol/UI when TTS is off and backend is missing.
5. Verify latency regression by proving the first answer does not enter scorer code.
6. Run focused and broad verification.
